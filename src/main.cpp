#include <iostream>

#include <forge.h>

int main(int argc, char **argv) {
  // forge::Window window(800, 600, "Hello world");

  // while (window.IsOpen()) {
  //   window.Clear(0.2f, 0.3f, 0.7f, 1.0f);

  //   window.Update();

  //   window.Draw();
  // }

  forge::Example();
  return 0;
}